# Exam 2

## Getting Started
1. Change a `.env.example` file to `.env` in the project root.
2. Set `GEMINI_API_KEY` to your Google Cloud API key for the Generative Language API.
3. Run the app:

```bash
flutter pub get
flutter run
```
